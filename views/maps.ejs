<!DOCTYPE html>
<html lang="en">

<%- include('partials/_head'); %>

<body class="maps-page">

  <%- include('partials/_nav'); %>

  <header class="page-header">
  </header>

  <main class="container">

  <section class="map-container">
    <header class="map-container-header">
      <h3></h3>
    </header>
    <!-- Shows map for each map in database -->
    <% for(let map of maps) { %>
      <article class="map">
        <header class="map-header">
          <span class="map-creator-name"><%= map.creator_name %></span>
        </header>
        <main class="map-main">
          <div class="map-thumbnail">image</div>
          <div class="map-details">
            <span class="name"><%= map.title %></span>
            <span class="location"><%= map.city %>, <%= map.country %></span>
            <span class="description"><%= map.description %></span>
          </div>
        </main>
        <footer class="map-footer">
          <div class="map-footer-left">
            <span class="map-date"><%= map.date_created %></span>
          </div>
          <div class="map-footer-right">
            <i class="fas fa-map-pin"> 0</i>
            <i class="far fa-star"></i>
          </div>
        </footer>
      </article>
    <% } %>
  </section>

  </main>

  <%- include('partials/_footer'); %>
</body>

</html>
